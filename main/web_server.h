#ifndef WEB_SERVER_H
#define WEB_SERVER_H

#include "config_manager.h"

// Inicializa el servidor y recibe el puntero a la estructura de configuración
void start_webserver(system_config_t *config);

#endif